module.exports = {
    indexOf: function(list, value) {
        return list.indexOf(value) > -1
    },


    selectFindByIndex: function(list, value) {
        console.log(list)
        return list.find(function(item) {
            item.value == value
        }).label
    }
}